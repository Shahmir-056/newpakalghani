// src/app/app.config.ts
// ✅ Everything is initialized in main.ts
// This file is kept empty to avoid duplicate Firebase initialization
import { ApplicationConfig } from '@angular/core';

export const appConfig: ApplicationConfig = {
  providers: []
};